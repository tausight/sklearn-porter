#include <stdlib.h>
#include <stdio.h>
#include <math.h>

{method}

float main(int argc, const char * argv[]) {{
    float features[argc-1];
    int i;
    for (i = 1; i < argc; i++) {{
        features[i-1] = atof(argv[i]);
    }}
    printf("%f", {method_name}(features));
    return 0;
}}